rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد تطوير مؤقتة: تسمح بالقراءة والكتابة للجميع
    // يجب تشديد هذه القواعد قبل الإطلاق الفعلي للإنتاج
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
