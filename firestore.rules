rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد حماية أولية: تسمح بالقراءة والكتابة للجميع (لأغراض التطوير MVP)
    // يجب تشديد هذه القواعد قبل الإطلاق الفعلي
    match /{document=**} {
      allow read, write: if true;
    }
  }
}